import React from 'react';
import './Auth.scss';
import LoginForm from './LoginForm/LoginForm';

const Auth = () => (
  (
    <div>
      <h1>Login page!</h1>
      <LoginForm />
    </div>
  )
);

export default Auth;
